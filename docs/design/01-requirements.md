# 가상 이커머스 기본 요구사항 명세서

---

## 목차
- [1. 서비스 개요](#1-서비스-개요)
- [2. 서비스 도메인 용어 정의](#2-서비스-도메인-용어-정의)
- [3. 주요 액터와 사용자 여정](#3-주요-액터와-사용자-여정)
  - [3.1 액터 카탈로그](#31-액터-카탈로그)
  - [3.2 핵심 사용자 여정](#32-핵심-사용자-여정)
  - [3.3 여정 ↔ API 매핑](#33-여정--api-매핑)
- [4. API 설계 제안 및 인증 가이드](#4-api-설계-제안-및-인증-가이드)
- [5. 상세 기능 요구사항](#5-상세-기능-요구사항)
  - [5.1 유저 (Users)](#51-유저-users)
  - [5.2 브랜드 & 상품 (Brands / Products)](#52-브랜드--상품-brands--products)
  - [5.3 브랜드 & 상품 ADMIN](#53-브랜드--상품-admin)
  - [5.4 좋아요 (Likes)](#54-좋아요-likes)
  - [5.5 주문 (Orders)](#55-주문-orders)
    - [5.5.1 사용자 주문 API](#551-사용자-주문-api)
    - [5.5.2 관리자 주문 조회 API](#552-관리자-주문-조회-api)
    - [5.5.3 주문 처리 핵심 비즈니스 룰](#553-주문-처리-핵심-비즈니스-룰)
    - [5.5.4 주문·결제 상태 정책](#554-주문결제-상태-정책)
    - [5.5.5 트랜잭션·보상 원칙](#555-트랜잭션보상-원칙)
  - [5.6 쿠폰 (Coupons)](#56-쿠폰-coupons)
    - [5.6.1 사용자 쿠폰 API](#561-사용자-쿠폰-api)
    - [5.6.2 관리자 쿠폰 API](#562-관리자-쿠폰-api)
    - [5.6.3 쿠폰 발급 규칙](#563-쿠폰-발급-규칙)
    - [5.6.4 쿠폰 사용·조회 규칙](#564-쿠폰-사용조회-규칙)
    - [5.6.5 쿠폰 할인 정책](#565-쿠폰-할인-정책)
    - [5.6.6 운영 감사 확장 후보](#566-운영-감사-확장-후보)
  - [5.7 이벤트 아키텍처와 상품 지표](#57-이벤트-아키텍처와-상품-지표)
- [6. 심화 과정](#6-심화-과정)

---

## 1. 서비스 개요

> [!NOTE]
> 본 프로젝트는 **좋아요**, **쿠폰**, **결제** 기능을 포함하는 **가상 이커머스 서비스**의 백엔드 시스템 구현을 목표로 한다.
> Round 2 핵심 설계 범위는 **브랜드/상품 조회, 좋아요, 주문, 재고, 결제 기록**이다.
> Round 4 핵심 설계 범위는 **쿠폰 생성·발급·조회·사용과 주문 시 쿠폰/재고 동시 정합성 보장**이다.
> Round 7 핵심 설계 범위는 **ApplicationEvent 기반 트랜잭션 이후 부가 처리, outbox/Kafka 이벤트 파이프라인, 상품 지표 projection, Kafka 기반 비동기 선착순 쿠폰 발급**이다.
> Payment 사가, 외부 결제 연동, 결제 실패 보상 회복은 후속 결제 연동 단계의 목표 설계다. Round 4 구현 범위는 쿠폰/재고/주문 트랜잭션 정합성에 한정하며, 별도 이슈가 없으면 Payment 도메인과 결제 게이트웨이를 새로 구현하지 않는다.
> 사용자 회원가입·내 정보 조회·비밀번호 변경은 현재 구현 패턴과 인증 전제를 설명하기 위한 레퍼런스로 포함한다.

---

## 2. 서비스 도메인 용어 정의

**도메인(Domain)이란?**

도메인은 **소프트웨어 서비스로 해결하고자 하는 사용자의 문제 영역이자 비즈니스 활동의 범위**다.
서비스가 시장에서 가치를 창출하는 가장 핵심적인 활동이 무엇인지 정의한 영역이며, 이 범위 내에서 활동하는 주체(Actor)와 행동(Behavior)들의 유기적인 관계를 포함한다.

**도메인 용어(Domain Language)란?**

기획자, 개발자, 사업 담당자 등 직군에 상관없이 서비스 내에서 자주 사용되고 비즈니스에 핵심적인 용어를 정의한 것이다.
용어 종류와 개념이 확립되지 않으면 같은 단어라도 서비스의 성격에 따라 용어의 정의나 범위, 역할이 상이할 수 있다.
또한 구성원마다 동일한 단어를 다르게 해석하여 논의와 작업 결과물의 정합성이 깨질 수 있다.
따라서 서비스 내의 명확한 기준으로 용어의 개념을 선언하여 이를 통해 **유비쿼터스 언어(Ubiquitous Language)**를 확립하고, 직군 간·개인 간의 개념을 일치시켜 프로젝트의 방향성과 구현 모델을 통일하는 효과를 얻는다.

### 도메인 용어 목록

#### 사용자

서비스를 이용하는 회원이다. 사용자는 회원가입을 통해 생성되며, 로그인 ID와 비밀번호를 통해 '로그인 필요 API'의 요청자로 식별된다.
서비스 대고객 회원인 '일반 사용자' 혹은 '사용자'와 '일반 사용자'와 '상품', '브랜드' 등 일반 사용자의 서비스 이용을 보조하는 '관리자' 두 유형으로 구분된다.

##### 값과 규칙

- 로그인 ID: 영문과 숫자로 이루어진 4~20자 문자열이며, 시스템 전체에서 유일해야 한다.
- 비밀번호: 8~16자 문자열이며, 영문 대문자, 영문 소문자, 숫자, 특수문자를 각각 1개 이상 포함해야 한다. 생년월일 토큰(`yyyyMMdd`, `yyMMdd`, `MMdd`)은 포함할 수 없으며, 저장 시에는 단방향으로 인코딩된 값만 보관한다.
- 이름: 공백이 아닌 1~50자 문자열이다.
- 생년월일: 과거 날짜만 허용한다.
- 이메일: 표준 이메일 형식을 따른다.

##### 행위 정의

- 회원가입: 서비스에 가입되지 않은 사용자가 서비스 이용에 필요한 개인정보와 식별자를 등록하여 계정을 새로 생성하는 행위다.
- 사용자 정보 조회: 로그인한 사용자가 자신의 계정 식별자와 암호를 바탕으로 자신의 정보를 조회하는 행위다.
- 비밀번호 변경: 로그인한 사용자가 현재 비밀번호를 검증한 뒤, 사용중인 현재 비밀번호와는 다른 새 비밀번호로 변경하는 행위다.

#### 상품

사용자가 탐색하고 좋아요를 표시하거나 주문할 수 있는 판매 대상이다. 상품은 하나의 브랜드에 속하며, 주문 시점에는 상품의 주요 정보가 주문 스냅샷으로 보관된다.

##### 값과 규칙

- 상품 식별자: 상품 상세 조회, 좋아요, 주문 항목에서 상품을 가리키는 식별 값이다.
- 브랜드: 상품은 등록된 브랜드에 속해야 하며, 한 번 등록된 상품의 브랜드는 임의로 수정할 수 없다.
- 상품명과 가격: 주문 시점에 주문 스냅샷으로 보관되어야 하는 대표 정보다. 상품명은 공백 문자열일 수 없다.
- 재고: 상품의 주문 가능 여부를 결정하는 수량이며, 주문 시점에 확인 및 차감되어야 한다.
- 목록 조회 조건: 브랜드 식별자로 필터링할 수 있고, 최신순(`latest`) 정렬은 필수로 제공한다. 가격 낮은 순(`price_asc`)과 좋아요 많은 순(`likes_desc`)은 선택적으로 확장할 수 있다.
- 페이지 조건: 상품 목록은 페이지 번호와 페이지당 상품 수를 기준으로 조회한다.

##### 행위 정의

- 상품 목록 조회: 사용자가 조건에 맞는 상품 목록을 조회하는 행위다.
- 상품 상세 조회: 사용자가 특정 상품 정보를 조회하는 행위다.
- 상품 등록: 관리자가 등록된 브랜드에 속한 상품을 새로 등록하는 행위다.
- 상품 정보 수정: 관리자가 등록된 상품 정보를 변경하는 행위다.
- 상품 삭제: 관리자가 등록된 상품을 삭제하는 행위다.

#### 브랜드

브랜드의 사전적 정의는 어떤 경제적인 생산자를 구별하는 지각된 이미지와 경험의 집합이며 보다 좁게는 어떤 상품이나 회사를 나타내는 상표, 표지이다.
이 서비스에서는 주로 상품이 소속된 그룹이나 집합의 역할을 수행하는 구별자로 정의하며, 상품은 특정 브랜드에 소속될 수 있다.
사용자는 브랜드 정보를 조회할 수 있고, 관리자는 브랜드를 등록하고 관리한다.

##### 값과 규칙

- 브랜드 식별자: 브랜드를 조회하거나 상품을 필터링할 때 사용하는 식별 값이다.
- 브랜드와 상품의 관계: 상품은 이미 시스템에 등록된 브랜드에만 연결될 수 있다.
- 브랜드 삭제: 브랜드가 삭제되면 해당 브랜드에 속한 상품들도 함께 삭제되어야 한다.

##### 행위 정의

- 브랜드 조회: 사용자가 특정 브랜드 정보를 조회하는 행위다.
- 브랜드 목록 조회: 관리자가 등록된 브랜드 목록을 조회하는 행위다.
- 브랜드 등록: 관리자가 새로운 브랜드를 시스템에 등록하는 행위다.
- 브랜드 정보 수정: 관리자가 등록된 브랜드 정보를 변경하는 행위다.
- 브랜드 삭제: 관리자가 등록된 브랜드와 그 브랜드에 속한 상품들을 삭제하는 행위다.

#### 좋아요
사용자가 특정 상품에 관심을 표시한 상태다. 좋아요는 사용자와 상품 사이의 관계로 표현되며, 사용자는 자신이 좋아요 한 상품 목록을 조회할 수 있다.
자신의 식별자이자 인증 정보인 계정이 있는 사용자가 로그인 후 특정 상품에 대하여 '좋아요 누르기'를 수행할 수 있으며, 한 사용자가 한 상품에 대하여 한 번만 수행할 수 있다.
`likes` 레코드는 사용자-상품 좋아요 상태의 진실 원천(source of truth)이다. 상품별 좋아요 수는 `product_metrics.like_count`로 조회하며, 이 값은 `likes` 변경 이벤트를 Kafka consumer가 반영하는 eventually consistent projection이다. 기존 `product_like_counts`의 의미는 `product_metrics.like_count`로 흡수되며, 독립 테이블로 남기더라도 마이그레이션 bridge로만 취급한다.

##### 값과 규칙

- 사용자: 좋아요를 등록하거나 취소하는 주체다.
- 상품: 좋아요의 대상이다.
- 좋아요 목록: 로그인한 사용자가 좋아요 한 상품들의 목록이다.
- 좋아요 수: 상품 목록·상세 조회와 `likes_desc` 정렬에 사용하는 `product_metrics.like_count` 읽기 모델이다. 쓰기 직후에는 projection lag 때문에 `likes`의 최신 상태보다 늦게 보일 수 있으며, 정확한 사용자별 좋아요 상태 판단은 `likes`를 기준으로 한다.

#### 상품 지표

상품 지표는 상품 목록·상세 조회, 정렬, 운영 분석에 필요한 파생 카운터다. `product_metrics`는 상품별 좋아요 수, 판매 수, 조회 수를 한 행으로 보관하는 rebuildable derived projection이다. 권위 상태는 `likes`, 주문/주문 항목, 상품 조회 이벤트 같은 원천 이벤트와 원천 테이블에 남는다.

##### 값과 규칙

- 상품 식별자: 지표가 귀속되는 상품 ID다.
- 좋아요 수: `likes` 상태 변경 이벤트의 증감량을 반영한 카운터다.
- 판매 수: 주문/결제 확정 또는 설계상 판매 반영 시점으로 선택한 주문 이벤트에서 파생되는 카운터다.
- 조회 수: 상품 상세 조회가 커밋된 뒤 발행되는 상품 조회 이벤트에서 파생되는 카운터다.
- 최신성 기준: `product_metrics`는 전체 row의 `last_event_at`와 함께 `last_like_event_at`, `last_sales_event_at`, `last_view_event_at`을 저장한다. consumer는 같은 지표의 이전 발생시각 이벤트만 stale로 무시하고, 서로 다른 지표 이벤트는 발생시각이 역전되어도 독립적으로 반영한다.

##### 행위 정의

- 지표 반영: Kafka consumer가 이벤트를 처리해 `product_metrics`를 upsert한다.
- 지표 재구성: consumer 장애나 누락 의심 시 원천 테이블과 이벤트 replay를 기준으로 projection을 다시 만든다.

##### 행위 정의

- 좋아요 등록: 로그인한 사용자가 특정 상품에 관심을 표시하는 행위다.
- 좋아요 취소: 로그인한 사용자가 특정 상품에 대한 관심 표시를 해제하는 행위다.
- 좋아요 목록 조회: 로그인한 사용자가 자신이 좋아요 한 상품 목록을 조회하는 행위다.

#### 주문
사용자가 상품을 구매하는 과정과 그 결과물을 말한다. 주문은 사용자와 상품을 연결하며, 주문 요청 시 상품 재고 확인 및 차감과 발급 쿠폰 사용 상태 변경이 정상적으로 보장되어야 한다.

##### 값과 규칙
- 주문자: 주문을 요청한 로그인 사용자다.
- 주문 기간: 주문 목록 조회 시 시작일과 종료일을 기준으로 조회할 수 있다.
- 주문 상세: 단일 주문의 결과와 포함된 주문 항목을 확인할 수 있는 정보다.
- 재고 정합성: 주문 시점에 상품 재고를 확인하고 차감해야 한다.
- 적용 쿠폰: 주문 요청에는 사용자가 보유한 발급 쿠폰 식별자를 외부 필드명 `couponId`(내부 `issuedCouponId`로 매핑)로 선택적으로 포함할 수 있다. 쿠폰을 적용하지 않는 주문은 이 필드를 생략한다.
- 쿠폰 정합성: 발급 쿠폰은 소유자, 사용 가능 상태, 유효기간, 최소 주문 금액을 검증한 뒤 주문 생성과 같은 트랜잭션에서 사용 처리되어야 한다.
- 할인 스냅샷: 주문에는 쿠폰 사용 결과로 확정된 할인 금액을 `discountPrice`로 저장한다. 이후 쿠폰 템플릿 정책이 변경되어도 과거 주문 금액은 바뀌지 않는다.
- 주문 상태: 주문은 `PAYMENT_PENDING`으로 생성되고, 외부 결제 결과에 따라 `ORDERED` 또는 `PAYMENT_FAILED`로 전이한다. `CANCELED`는 취소/환불 정책이 확정될 때 사용하는 확장 상태다.
- 결제 기록: 후속 결제 연동 설계에서는 주문마다 하나의 결제 기록을 둔다. 주문 요청 시 결제 수단, 결제 금액, 결제 상태를 남기며, 외부 결제 시스템 연동 방식은 port로 분리한다. Round 4 구현 범위에서는 결제 기록 생성과 외부 결제 연동을 필수 대상으로 보지 않는다.

##### 주문 항목

주문에 포함된 개별 상품 구매 단위다. 하나의 주문은 하나 이상의 주문 항목을 가질 수 있고, 각 주문 항목은 주문할 상품과 수량을 가진다.

###### 값과 규칙

- 상품 식별자: 주문 항목이 어떤 상품을 구매하려는지 나타내는 값이다.
- 수량: 해당 상품을 몇 개 주문할지 나타내는 값이다.
- 주문 시점 정보: 주문 항목은 주문 스냅샷을 통해 주문 당시의 상품명, 가격 등의 정보를 보존할 수 있다.

###### 행위 정의

- 주문 항목 구성: 사용자가 주문 요청 본문에 상품 식별자와 수량을 담아 구매 대상을 구성하는 행위다.

##### 행위 정의

- 주문 요청: 로그인한 사용자가 하나 이상의 상품과 수량을 지정해 구매를 요청하는 행위다.
- 쿠폰 적용 주문 요청: 로그인한 사용자가 자신에게 발급된 사용 가능 쿠폰을 지정해 할인 적용과 주문 생성을 함께 요청하는 행위다.
- 주문 목록 조회: 로그인한 사용자가 자신의 주문 목록을 조회하는 행위다.
- 주문 상세 조회: 로그인한 사용자가 자신의 특정 주문 상세를 조회하는 행위다.
- 주문 관리자 조회: 관리자가 주문 목록 또는 단일 주문 상세를 조회하는 행위다.

#### 쿠폰

사용자가 주문 전에 발급받아 주문에 사용할 수 있는 혜택이다. Round 4에서는 관리자 정의인 **쿠폰 템플릿**과 사용자 보유 상태인 **발급 쿠폰**을 분리한다. 템플릿 정책은 앞으로 바뀔 수 있지만, 이미 생성된 주문의 할인 스냅샷은 바뀌지 않아야 한다.

##### 쿠폰 템플릿

##### 값과 규칙

- 쿠폰명: 관리자가 식별할 수 있는 공백이 아닌 이름이다.
- 할인 방식: 외부 API는 원문 계약인 `FIXED`, `RATE`를 사용하고, 내부 도메인 정책은 각각 `FIXED_AMOUNT`, `PERCENTAGE`로 매핑한다.
- 할인 값: 외부 API의 `value`와 내부/DB의 `discount_value`는 정수다. 정액 할인은 원 단위 할인 금액, 정률 할인은 1~100 범위의 퍼센트 정수를 의미한다.
- 최소 주문 금액: 쿠폰을 사용할 수 있는 주문 총액의 하한이다. 주문 총액이 기준보다 작으면 사용할 수 없다.
- 만료 시각: 발급 쿠폰의 사용 가능 기한이다. 만료된 쿠폰은 사용할 수 없다.
- 삭제 정책: 쿠폰 템플릿 삭제는 soft delete로 처리한다. 이미 발급된 쿠폰과 과거 주문 스냅샷은 유지한다.

##### 행위 정의

- 쿠폰 템플릿 생성: 관리자가 할인 방식, 할인 값, 최소 주문 금액, 만료 시각을 지정해 쿠폰 정책을 등록하는 행위다.
- 쿠폰 템플릿 수정: 관리자가 아직 삭제되지 않은 쿠폰 템플릿의 운영 정보를 변경하는 행위다. 과거 주문 할인 스냅샷에는 영향을 주지 않는다.
- 쿠폰 템플릿 삭제: 관리자가 쿠폰 템플릿을 더 이상 발급 대상으로 노출하지 않도록 soft delete하는 행위다.
- 쿠폰 할인 계산: 주문 총액과 할인 방식을 기준으로 실제 할인 금액을 계산하는 행위다. 정률 할인은 `BigDecimal` 중간 계산 후 `RoundingMode.FLOOR`로 원 단위 정수화하며, 할인 금액은 주문 총액을 초과할 수 없다.

##### 발급 쿠폰

사용자가 특정 쿠폰 템플릿을 발급받은 상태다.

###### 값과 규칙

- 쿠폰 소유자: 쿠폰을 발급받은 사용자다.
- 쿠폰 템플릿: 발급 쿠폰이 어떤 관리자 정의에서 파생되었는지 나타내는 기준이다.
- 발급 유일성: 한 사용자는 같은 쿠폰 템플릿을 한 번만 발급받을 수 있다.
- 사용 상태: 저장 상태는 `AVAILABLE`, `USED`를 둔다. `EXPIRED`는 저장 상태가 아니라 조회 시 `expiredAt`과 현재 시각을 비교해 계산한 표시 상태다.
- 사용 시각: 쿠폰이 주문에 적용되어 사용 처리된 시각이다.

###### 행위 정의

- 쿠폰 발급: 일반 사용자가 발급 가능한 쿠폰 템플릿을 자신의 발급 쿠폰으로 생성하는 행위다.
- 내 쿠폰 조회: 일반 사용자가 자신에게 발급된 쿠폰 목록을 조회하는 행위다. 응답에는 `AVAILABLE`, `USED`, `EXPIRED` 표시 상태가 포함된다.
- 쿠폰 사용: 주문 과정에서 발급 쿠폰을 검증하고 `USED` 상태로 전환하는 행위다. 주문 생성, 재고 차감, 쿠폰 사용은 하나의 원자적 트랜잭션으로 처리한다.

#### 결제

주문에 대한 지불 처리다. Round 2 설계에서는 구체적인 결제수단별 상세 정책까지 확정하지 않지만, 주문과 연결된 결제 요청/승인/실패 기록은 남긴다.
이 절의 사가와 외부 결제 호출 흐름은 후속 결제 연동 단계의 목표 설계이며, Round 4 쿠폰/동시성 구현 범위에 포함하지 않는다.

##### 값과 규칙

- 결제 대상: 결제는 특정 주문을 대상으로 한다.
- 결제 수단: 카드, 간편결제 등 구체적인 수단은 추후 확정하되, 현재 설계에서는 `PaymentMethod` 값으로 기록한다.
- 결제 상태: 결제 요청(`REQUESTED`), 승인(`APPROVED`), 실패(`FAILED`)를 최소 상태로 둔다.
- 결제 금액: 주문의 최종 결제 금액과 일치해야 한다.
- 외부 거래 식별자: 외부 결제 시스템이 거래 ID를 반환하면 결제 기록에 보관한다.
- 결제 기록: 후속 결제 연동 구현 시 주문과 결제 기록을 1:1로 둔다. 같은 주문에 대한 다중 결제 시도와 재시도 이력은 결제 정책이 확장될 때 1:N으로 전환한다.

##### 행위 정의

- 결제 요청 기록: 주문 생성 시점에 결제 요청 상태를 저장하는 행위다.
- 결제 승인 기록: 외부 결제 시스템 승인 결과를 결제 기록에 반영하는 행위다.
- 결제 실패 기록: 외부 결제 시스템 거절/실패 결과와 사유를 결제 기록에 반영하는 행위다.

##### 트랜잭션 경계

- 외부 결제 시스템 호출은 어떤 DB 트랜잭션에도 포함하지 않는다. DB 커넥션 점유, 재고 락 경합, 외부 시스템과 DB 상태의 발산을 막기 위함이다.
- 주문 요청은 `(TX1: 주문·재고·쿠폰 적용 시 쿠폰 사용·결제 요청 기록) → 외부 호출 → (TX2: 결제 결과 반영과 보상)` 의 3단 구조로 처리한다.
- 결제 실패 보상은 **트랜잭션 롤백이 아니라 보상 트랜잭션**이다. TX1 이 이미 커밋되어 자동 원복할 수 없기 때문이다.
- TX2 실패 보상은 주문 상태가 `PAYMENT_PENDING`일 때만 수행한다. 이 상태 가드로 중복 콜백이나 회복 프로세스의 재고·쿠폰 이중 복구를 막는다.
- 결제 실패 시 결제 기록은 `FAILED`, 주문은 `PAYMENT_FAILED`로 남긴다. 재고를 복구하고, 쿠폰 적용 주문이면 발급 쿠폰을 `AVAILABLE`로 되돌린 뒤 실패 주문의 `issuedCouponId` 연결을 해제한다.

##### 확장 판단

- 결제수단별 승인/취소 정책, 비동기 승인, 재시도, 웹훅 수신처럼 외부 연동 안정성이 중요해지면 outbox 패턴을 추가한다.
- 같은 주문에 대한 재결제나 다중 결제 시도 이력이 필요해지면 주문-결제 관계를 1:N으로 확장한다.
- 현재 단계에서는 `payments` 결제 기록 테이블까지만 확정하고, outbox 테이블은 결제수단과 연동 방식이 구체화된 뒤 설계한다.

#### 재고

상품의 주문 가능 수량이다. 재고는 주문 가능 여부를 판단하는 기준이며, 주문 요청이 처리될 때 확인되고 차감되어야 한다.

##### 값과 규칙

- 상품: 재고가 귀속되는 대상이다.
- 주문 가능 수량: 주문 시점에 구매 가능한 상품 수량이다.
- 정합성: 주문 시점의 재고 확인과 차감은 정상적으로 보장되어야 한다.

##### 행위 정의

- 재고 확인: 주문 요청 시 상품을 주문할 수 있는 수량이 남아 있는지 확인하는 행위다.
- 재고 차감: 주문이 처리될 때 주문 수량만큼 상품 재고를 줄이는 행위다.

#### 주문 스냅샷

주문 시점의 상품 정보를 주문에 보관한 기록이다. 상품 정보가 이후 변경되더라도 과거 주문 내역은 주문 당시의 정보를 기준으로 유지되어야 한다.

##### 값과 규칙

- 상품명: 주문 시점의 상품 이름이다.
- 가격: 주문 시점의 상품 가격이다.
- 보존 기준: 주문 이후 상품명이나 가격이 변경되더라도 기존 주문 내역은 훼손되지 않아야 한다.
- 확장 정보: 주문 당시 필요한 상품 노출 정보는 추후 요구사항에 따라 함께 보관할 수 있다.

##### 행위 정의

- 주문 스냅샷 생성: 주문이 생성될 때 주문 당시의 상품 정보를 함께 저장하는 행위다.
- 주문 스냅샷 조회: 주문 상세 조회에서 주문 당시의 상품 정보를 확인하는 행위다.

---

## 3. 주요 액터와 사용자 여정

본 절은 시스템이 어떤 사용자(액터)를 가정하며, 그 사용자가 어떤 흐름으로 서비스를 이용하는지를 정리한다.
서비스에 필요한 구체화된 개별 API 명세는 이 여정에서 도출된다.

### 3.1 액터 카탈로그

| 액터 | 식별 방식 | 권한 범위 | 주요 접점 |
| :--- | :--- | :--- | :--- |
| **익명 방문자** | 인증 헤더 없음 | 공개 정보 조회만 가능 (브랜드/상품) | 상품 탐색, 회원가입 |
| **일반 사용자** | `X-Loopers-LoginId` + `X-Loopers-LoginPw` | 자신의 자원에 한해 조회·변경 가능 (내 정보, 내 좋아요, 내 주문, 내 쿠폰) | 좋아요, 쿠폰 발급·조회, 주문, 비밀번호 변경 |
| **관리자** | `X-Loopers-Ldap` | 브랜드/상품/쿠폰/주문에 대한 등록·수정·삭제·전체 조회 권한 | 카탈로그 운영, 쿠폰 운영, 주문 모니터링 |

> [!IMPORTANT]
> 일반 사용자는 **타 사용자의 자원**(다른 사람의 좋아요 목록, 다른 사람의 주문)에 접근할 수 없다. 내부적으로는 단순 자원 없음과 구분하여 **인가 실패**로 분류·기록하되, 외부 응답은 자원 존재 여부를 노출하지 않기 위해 `404`로 반환한다 (§4 HTTP 응답 코드 정책 참조).

### 3.2 핵심 사용자 여정

각 여정은 단계 2에서 시나리오 단위로 상세화(사전조건·정상경로·예외경로·인수기준)된다. 본 단계는 흐름의 뼈대만 정의한다.

여정 식별자 명명 규칙
- 접두사는 **주도 액터**(여정의 시작·주된 행동의 주체)를 기준으로 한다. 여정 안에서 다른 액터가 등장할 수 있다.
- 정상 여정: `<액터>-J<번호>`  (예: `User-J1`)
- 예외/실패 여정: `<액터>-E<번호>`  (예: `User-E1`)

#### 일반 사용자 여정 (User-J / User-E)

##### User-J1. 신규 가입자가 첫 주문을 완료한다
1. 익명 방문자가 회원가입 → 일반 사용자로 전환
2. 로그인 헤더로 상품 목록·상세 조회
3. 관심 상품에 좋아요
4. 주문 항목 구성 → 주문 요청 → 재고 확인·차감 → 주문 확정
5. 주문 상세 조회로 결과 확인

##### User-J2. 재방문 사용자가 좋아요 목록에서 다시 주문한다
1. 일반 사용자가 자신의 좋아요 목록 조회
2. 목록에서 상품 상세 재확인
3. 주문 항목 구성 → 주문 요청 → 확정
4. 주문 목록 조회(기간 조건)로 과거 주문 확인

##### User-J3. 비밀번호를 변경한다
1. 일반 사용자가 로그인 헤더로 자신의 정보 조회
2. 현재 비밀번호 + 새 비밀번호 제출
3. 현재 비밀번호 일치 확인 → 새 비밀번호 정책 검증(길이·구성·생년월일 토큰 금지·현재 비밀번호와 상이) → 단방향 인코딩 후 저장
4. 변경 결과 응답

##### User-J4. 상품에 좋아요를 토글한다 (멱등 보장)
좋아요는 사용자×상품 쌍의 **상태 기계**다. 동일 요청을 반복해도 동일한 최종 상태가 보장된다.
- 미좋아요 상태에서 `POST /likes` → 좋아요 상태로 전이
- 좋아요 상태에서 `POST /likes` → 좋아요 상태 유지 (멱등)
- 좋아요 상태에서 `DELETE /likes` → 미좋아요 상태로 전이
- 미좋아요 상태에서 `DELETE /likes` → 미좋아요 상태 유지 (멱등)
- 좋아요 목록 조회로 현재 좋아요 상태 확인

##### User-J5. 쿠폰을 발급받아 주문에 적용한다
1. 일반 사용자가 발급 가능한 쿠폰 목록을 조회한다
2. 원하는 쿠폰 템플릿을 선택해 발급 요청을 접수한다
3. 시스템은 요청 식별자와 `PENDING` 상태를 `202 Accepted`로 반환한다
4. 실제 발급은 `coupon-issue-requests` Kafka topic을 소비하는 worker가 별도 트랜잭션에서 처리한다
5. 사용자는 발급 요청 상태 polling API로 `PENDING`/`ISSUED`/`DUPLICATE`/`SOLD_OUT`/`FAILED`를 확인한다
6. 내 쿠폰 목록에서 발급 완료 쿠폰과 표시 상태(`AVAILABLE`/`USED`/`EXPIRED`)를 확인한다
7. 주문 요청에 `issuedCouponId`를 포함한다
8. 시스템은 발급 쿠폰 소유자·상태·유효기간·최소 주문 금액을 검증하고, 재고 차감과 쿠폰 사용 처리를 하나의 트랜잭션으로 확정한다
9. 주문 상세에서 총액, 할인 금액, 최종 결제 금액, 적용 쿠폰 식별자를 확인한다

##### User-E1. 타인 자원에 접근하려다 거부된다
- 일반 사용자 A가 사용자 B의 좋아요 목록 또는 주문 상세를 조회 시도
- 시스템은 내부적으로 **인가 실패**로 분류·기록하고, 외부 응답은 B의 자원 존재 여부를 노출하지 않도록 `404`로 반환한다 (§4 HTTP 응답 코드 정책 참조)
- 회복: A는 자신의 자원 경로로 재요청

##### User-E2. 재고 부족으로 주문이 거부된다
- 주문 요청 시점에 주문 항목 중 하나라도 재고가 요청 수량보다 적음
- 시스템은 **주문 전체를 거부**하고, 어떤 항목의 재고도 차감하지 않는다 (부분 성공 금지)
- 사용자에게 부족 항목과 가용 수량을 통지
- 회복: 사용자는 수량을 조정하거나 해당 항목을 제거하여 재요청

##### User-E3. 결제 승인 실패로 주문이 완료되지 않는다
- TX1에서 주문이 `PAYMENT_PENDING`으로 생성되고 재고 차감, 쿠폰 사용 처리, 결제 요청 기록이 커밋된 뒤 외부 결제 시스템이 승인을 거절하거나 실패를 반환함
- 시스템은 결제 기록을 `FAILED`, 주문 상태를 `PAYMENT_FAILED`로 남긴다
- 보상은 주문 상태가 아직 `PAYMENT_PENDING`일 때만 수행한다. 재고를 복구하고, 쿠폰 적용 주문이면 발급 쿠폰을 `AVAILABLE`로 복구한 뒤 실패 주문의 `issuedCouponId`를 `NULL`로 분리한다
- 주문 row와 주문 금액 스냅샷은 실패 이력으로 유지한다. 주문 요청 API 응답은 성공 생성이 아니라 결제 승인 거절은 `409`, 외부 결제 장애는 `502`로 반환한다
- 회복: 사용자는 주문 목록이나 상세에서 `PAYMENT_FAILED` 상태를 확인하고, 결제수단을 바꾸거나 같은 주문 조건으로 새로 주문한다

##### User-E4. 쿠폰 사용 조건 불일치로 주문이 거부된다
- 주문 요청의 `issuedCouponId`가 존재하지 않거나 요청 사용자 소유가 아님
- 발급 쿠폰이 이미 `USED` 상태이거나 유효기간이 지남
- 주문 총액이 쿠폰 템플릿의 최소 주문 금액보다 작음
- 시스템은 주문 전체를 거부하고, 재고 차감과 쿠폰 사용 상태 변경을 모두 롤백한다
- 회복: 사용자는 사용 가능한 다른 발급 쿠폰을 선택하거나 쿠폰 없이 재요청

#### 관리자 여정 (Admin-J)

##### Admin-J1. 신규 브랜드·상품을 등록해 노출시킨다
1. 관리자가 브랜드 등록
2. 등록된 브랜드 식별자로 상품 등록 (브랜드 미등록 시 거부)
3. 일반 사용자/익명 방문자 측에서 신규 상품이 목록·상세에 노출되는지 확인

##### Admin-J2. 브랜드를 삭제하면 소속 상품이 함께 사라진다
1. 관리자가 브랜드 삭제 요청
2. 해당 브랜드에 속한 모든 상품이 브랜드와 함께 삭제됨
3. 일반 사용자 측에서 해당 상품들의 상세·목록 조회 시 더 이상 노출되지 않음
4. 과거 주문 내역의 상품 정보는 **주문 스냅샷**으로 보존되어 훼손되지 않음

##### Admin-J3. 등록된 카탈로그를 운영한다 (수정·단건 삭제)
1. 관리자가 등록된 브랜드 정보를 수정
2. 관리자가 등록된 상품 정보를 수정 (단, 한 번 등록된 상품의 브랜드는 변경 불가)
3. 관리자가 상품을 단건 삭제 (Admin-J2의 브랜드 cascade와 달리 해당 상품만 제거되며 브랜드 자체와 다른 상품은 유지)
4. 일반 사용자/익명 방문자 측에서 수정·삭제 결과가 목록·상세 조회에 반영되는지 확인
5. 과거 주문 내역의 상품 정보는 **주문 스냅샷**으로 보존되어 훼손되지 않음

##### Admin-J4. 주문 운영 현황을 모니터링한다
1. 관리자가 전체 주문 목록을 페이징 조회
2. 이상이 의심되거나 확인이 필요한 주문을 단건 상세 조회
3. (이상 주문에 대한 후속 조치 — 환불·취소 등 — 는 결제 연동 단계에서 확장)

##### Admin-J5. 쿠폰 템플릿을 운영한다
1. 관리자가 쿠폰 템플릿을 생성한다
2. 운영 정책 변경이 필요하면 쿠폰 템플릿 정보를 수정한다
3. 발급 중단이 필요하면 쿠폰 템플릿을 soft delete한다
4. 관리자가 쿠폰 템플릿 목록·상세와 발급 이력을 조회한다
5. 삭제된 템플릿은 신규 발급 대상에서 제외되지만, 이미 발급된 쿠폰과 주문 스냅샷은 유지된다

### 3.3 여정 <-> API 매핑

각 API가 어느 여정에서 호출되는지 매트릭스로 정리한다. 모든 §5의 API는 최소 하나의 여정에 등장한다 (고아 API 없음).

열 헤더 축약 표기: `U-J1`=User-J1, `U-E1`=User-E1, `A-J1`=Admin-J1 (정식 식별자는 §3.2를 따른다).

| API | U-J1 | U-J2 | U-J3 | U-J4 | U-E1 | U-E2 | U-E3 | A-J1 | A-J2 | A-J3 | A-J4 |
| :--- | :---: | :---: | :---: | :---: | :---: | :---: | :---: | :---: | :---: | :---: | :---: |
| `POST /api/v1/users` | ● | | | | | | | | | | |
| `GET /api/v1/users/me` | ● | ● | ● | | | | | | | | |
| `PUT /api/v1/users/password` | | | ● | | | | | | | | |
| `GET /api/v1/brands/{brandId}` | ● | ● | | | | | | ● | ● | ● | |
| `GET /api/v1/products` | ● | | | | | | | ● | ● | ● | |
| `GET /api/v1/products/{productId}` | ● | ● | | ● | | | | ● | ● | ● | |
| `POST /api/v1/products/{productId}/likes` | ● | | | ● | | | | | | | |
| `DELETE /api/v1/products/{productId}/likes` | | ● | | ● | | | | | | | |
| `GET /api/v1/users/{userId}/likes` | | ● | | ● | ● | | | | | | |
| `POST /api/v1/orders` | ● | ● | | | | ● | ● | | | | |
| `GET /api/v1/orders` | | ● | | | | | | | | | |
| `GET /api/v1/orders/{orderId}` | ● | | | | ● | | ● | | ● | | |
| `POST /api-admin/v1/brands` | | | | | | | | ● | | | |
| `PUT /api-admin/v1/brands/{brandId}` | | | | | | | | | | ● | |
| `DELETE /api-admin/v1/brands/{brandId}` | | | | | | | | | ● | | |
| `GET /api-admin/v1/brands` 목록·상세 | | | | | | | | ● | ● | ● | |
| `POST /api-admin/v1/products` | | | | | | | | ● | | | |
| `PUT /api-admin/v1/products/{productId}` | | | | | | | | | | ● | |
| `DELETE /api-admin/v1/products/{productId}` | | | | | | | | | | ● | |
| `GET /api-admin/v1/products` 목록·상세 | | | | | | | | ● | ● | ● | |
| `GET /api-admin/v1/orders` 목록·상세 | | | | | | | | | | | ● |

> [!NOTE]
> 위 매트릭스는 Round 2 API의 여정 매핑이다. 단계 2에서 §5 각 엔드포인트에 **명세 박스**(관련 여정 · 사전조건/권한 · 요청·응답 스키마 · 예외 응답 · 비즈니스 룰)를 부착하여 트레이서빌리티를 완성한다.

#### Round 4 쿠폰 API 매핑

| API | U-J5 | U-E4 | A-J5 |
| :--- | :---: | :---: | :---: |
| `POST /api/v1/coupons/{couponId}/issue` | ● | | |
| `GET /api/v1/coupons/issue-requests/{requestId}` | ● | | |
| `GET /api/v1/users/me/coupons` | ● | ● | |
| `POST /api/v1/orders` with `couponId` | ● | ● | |
| `POST /api-admin/v1/coupons` | | | ● |
| `PUT /api-admin/v1/coupons/{couponId}` | | | ● |
| `DELETE /api-admin/v1/coupons/{couponId}` | | | ● |
| `GET /api-admin/v1/coupons` 목록·상세 | | | ● |
| `GET /api-admin/v1/coupons/{couponId}/issues` | | | ● |

> [!NOTE]
> `GET /api/v1/coupons` 발급 가능 쿠폰 목록은 사용자 편의용 확장 후보지만 Round 4 원문 필수 API가 아니다. 별도 이슈가 없으면 구현 범위에 포함하지 않는다.

---

## 4. API 설계 제안 및 인증 가이드

### API Endpoint Prefix
* **대고객 서비스**: `/api/v1` prefix를 통해 제공된다.
* **관리자(Admin) 서비스**: `/api-admin/v1` prefix를 통해 제공된다.

### 인증 및 사용자 식별 방식
본 프로젝트는 학습용 프로젝트이므로 복잡한 실 인증/인가(Spring Security, JWT 등)는 연동하지 않는다. 대신 아래의 **HTTP 헤더**를 활용하여 요청자를 식별한다.

> [!IMPORTANT]
> - 인증/인가 로직 자체는 주요 범위가 아니므로 명세대로 헤더 파싱 형태로만 구현한다.
> - 유저는 타 유저의 정보에 직접 접근할 수 없도록 철저히 검증 및 제어해야 한다.

#### 대고객 기능 인증 헤더 (로그인 필요 API에 사용)
| HTTP Header Key | Description | Example |
| :--- | :--- | :--- |
| **`X-Loopers-LoginId`** | 로그인 ID | `user123` |
| **`X-Loopers-LoginPw`** | 비밀번호 | `securepass` |

#### 어드민 기능 인증 헤더 (관리자 전용 API에 사용)
| HTTP Header Key | Description | Example |
| :--- | :--- | :--- |
| **`X-Loopers-Ldap`** | 관리자 식별용 LDAP 정보 | `loopers.admin` |

### HTTP 응답 코드 정책

본 서비스는 별도의 커스텀 에러 코드 체계를 두지 않고 **표준 HTTP 상태 코드만** 사용한다.
도메인 계층은 비즈니스 예외를 던지는 책임만 지며, **표준 코드로의 매핑은 API 어댑터 계층에서 일관되게 수행**한다.

#### 상황 → 표준 코드 매핑

| 상황 | 코드 | 적용 예시 |
| :--- | :---: | :--- |
| 조회·변경 정상 처리 | `200 OK` | 내 정보 조회, 비밀번호 변경 성공, 상품·브랜드 조회 |
| 비동기 요청 접수 | `202 Accepted` | 쿠폰 발급 요청 접수 |
| 자원 생성 성공 | `201 Created` | 회원가입, 주문 요청, 브랜드 등록, 상품 등록 |
| 삭제 등 응답 본문 없는 성공 | `204 No Content` | 좋아요 취소, 브랜드 삭제, 상품 삭제 |
| 요청 형식·필수값·필드 정책 위반 | `400 Bad Request` | JSON 파싱 실패, 타입 오류, 필수 필드 누락, 로그인 ID/비밀번호/이름 입력 규칙 위반, 미래 생년월일, 잘못된 이메일 형식, 잘못된 정렬 키, 현재 비밀번호와 동일한 신규 비밀번호 |
| 인증 헤더 누락 또는 자격 불일치 | `401 Unauthorized` | 로그인 헤더 누락, 비밀번호 불일치, LDAP 헤더 누락 |
| 자원이 존재하지 않거나 본인 외 자원 접근 | `404 Not Found` | 존재하지 않는 `brandId`/`productId`/`orderId`, 타인의 좋아요 목록·주문 조회 시도 |
| 시스템 상태와의 비즈니스 규칙 충돌 | `409 Conflict` | 로그인 ID 중복, 재고 부족, 쿠폰 중복 발급, 이미 사용된 쿠폰, 만료된 쿠폰, 쿠폰 최소 주문 금액 미달, 결제 승인 거절, 미등록 브랜드로 상품 등록, 등록된 상품의 브랜드 변경 시도 |
| 외부 시스템 연동 실패 | `502 Bad Gateway` | 결제 시스템 타임아웃, 결제 시스템 장애 |
| 서버 내부 오류 | `500 Internal Server Error` | 처리 중 예기치 못한 예외 |

#### 운용 원칙

- **본인 외 자원 접근은 `403`이 아닌 `404`로 응답**하여 자원의 존재 여부를 노출하지 않는다 (§3.1, §3.2 `User-E1` 참조).
- **`400` vs `409` 구분 기준**: 단일 요청만으로 판단 가능한 *형식·필드 정책* 위반은 `400`, 시스템의 현재 상태(다른 자원·재고·이력)와의 *충돌*은 `409`로 매핑한다. 예외 — **비밀번호 정책 검증 일체는 `400`으로 묶는다**(현재 비밀번호와의 동일성 비교 포함). 이는 `agent_rules/projectInfo.md`의 비밀번호 변경 API 명세와의 일관성 유지를 위함이다.
- **좋아요 멱등 응답**: `POST /api/v1/products/{productId}/likes`는 신규/기존 상관없이 `200 OK`, `DELETE`는 신규/기존 상관없이 `204 No Content`로 응답한다 (§3.2 `User-J4` 참조).
- 응답 본문에는 사람이 읽을 수 있는 메시지만 포함하고, 내부 예외 클래스·스택·식별자 등 구현 세부는 노출하지 않는다.
- 구체적인 응답 바디 포맷은 클래스 다이어그램·구현 단계에서 확정한다.

---

## 5. 상세 기능 요구사항

### 5.1 유저 (Users)

| Method | URI | User Required | 설명 |
| :--- | :--- | :---: | :--- |
| `POST` | `/api/v1/users` | X | 회원가입 |
| `GET` | `/api/v1/users/me` | O | 내 정보 조회 |
| `PUT` | `/api/v1/users/password` | O | 비밀번호 변경 |

---

### 5.2 브랜드 & 상품 (Brands / Products)

| Method | URI | User Required | 설명 |
| :--- | :--- | :---: | :--- |
| `GET` | `/api/v1/brands/{brandId}` | X | 브랜드 정보 조회 |
| `GET` | `/api/v1/products` | X | 상품 목록 조회 |
| `GET` | `/api/v1/products/{productId}` | X | 상품 정보 조회 |

#### 상품 목록 조회 쿼리 파라미터 (Query Parameters)
| Parameter | Example | Default Value | Description |
| :--- | :--- | :---: | :--- |
| `brandId` | `1` | - | 특정 브랜드의 상품만 필터링 |
| `sort` | `latest` | `latest` | 정렬 기준 (`latest` / `price_asc` / `likes_desc`) |
| `page` | `0` | `0` | 페이지 번호 |
| `size` | `20` | `20` | 페이지당 상품 수 |

> [!TIP]
> **정렬 기준(sort) 구현 가이드:**
> * 정렬 기준 구현 범위는 유연하게 설계하되, **최신순(`latest`)**은 필수적으로 구현한다.
> * 그 외 가격 낮은 순(`price_asc`), 좋아요 많은 순(`likes_desc`) 정도는 상황에 따라 선택적으로 추가 구현한다.

---

### 5.3 브랜드 & 상품 ADMIN

| Method | URI | LDAP Required | 설명 |
| :--- | :--- | :---: | :--- |
| `GET` | `/api-admin/v1/brands?page=0&size=20` | O | 등록된 브랜드 목록 조회 |
| `GET` | `/api-admin/v1/brands/{brandId}` | O | 브랜드 상세 조회 |
| `POST` | `/api-admin/v1/brands` | O | 브랜드 등록 |
| `PUT` | `/api-admin/v1/brands/{brandId}` | O | 브랜드 정보 수정 |
| `DELETE` | `/api-admin/v1/brands/{brandId}` | O | 브랜드 삭제 <br>*브랜드 삭제 시 해당 브랜드의 상품들도 함께 영속적으로 삭제되어야 한다.* |
| `GET` | `/api-admin/v1/products?page=0&size=20&brandId={brandId}` | O | 등록된 상품 목록 조회 |
| `GET` | `/api-admin/v1/products/{productId}` | O | 상품 상세 조회 |
| `POST` | `/api-admin/v1/products` | O | 상품 등록 <br>*상품의 브랜드는 이미 시스템에 등록되어 있는 브랜드여야 한다.* |
| `PUT` | `/api-admin/v1/products/{productId}` | O | 상품 정보 수정 <br>*한 번 등록된 상품의 브랜드는 임의로 수정할 수 없다.* |
| `DELETE` | `/api-admin/v1/products/{productId}` | O | 상품 삭제 |

#### 관리자 변경 작업 이력

- 관리자의 브랜드/상품 변경 작업은 작업 이력으로 기록한다.
- 기록 대상은 브랜드 등록/수정/삭제, 상품 등록/수정/삭제처럼 데이터 상태를 바꾸는 작업으로 한정한다.
- 변경 대상 검증과 실제 변경 저장이 성공한 뒤 같은 유스케이스 경계 안에서 기록한다. 실패한 변경 요청은 성공 이력으로 기록하지 않는다.
- 브랜드/상품 목록 조회, 상세 조회, 주문 조회 같은 읽기 작업은 이력 기록 대상에서 제외한다.
- 이력에는 관리자 식별자, 대상 유형(브랜드/상품), 대상 식별자, 작업 유형(등록/수정/삭제), 작업 시각을 포함한다.

> [!NOTE]
> **설계 고민 포인트:**
> 상품 및 브랜드 정보 중에서 대고객(사용자) 화면과 어드민 관리자 화면에 각각 어떤 수준의 정보가 제공되어야 할지(예: 노출 여부, 내부 원가 정보 등) 깊이 고민하고 API 응답 스펙을 다르게 설계해 본다.

---

### 5.4 좋아요 (Likes)

| Method | URI | User Required | 설명 |
| :--- | :--- | :---: | :--- |
| `POST` | `/api/v1/products/{productId}/likes` | O | 상품 좋아요 등록 |
| `DELETE` | `/api/v1/products/{productId}/likes` | O | 상품 좋아요 취소 |
| `GET` | `/api/v1/users/{userId}/likes` | O | 내가 좋아요 한 상품 목록 조회 |

> [!IMPORTANT]
> `GET /api/v1/users/{userId}/likes`의 path `userId`는 **인증된 사용자 식별자와 반드시 일치**해야 한다. 불일치 시 자원 존재 여부를 노출하지 않도록 `404`로 응답한다 (§3.1, §3.2 `User-E1`, §4 HTTP 응답 코드 정책 참조).

#### 좋아요 수 projection 이벤트

- `likes`는 사용자-상품 좋아요 상태의 source of truth이고, `product_metrics.like_count`는 `LIKE_COUNT_CHANGED_V1` 이벤트를 Kafka consumer가 반영하는 eventually consistent projection이다.
- 좋아요 등록이 실제 상태 전이를 만들면(`likes` INSERT 영향 행 수 `1`) `delta = +1` 이벤트를 남긴다. 이미 좋아요 상태인 반복 `POST`처럼 영향 행 수가 `0`인 no-op 요청은 이벤트를 남기지 않는다.
- 좋아요 취소가 실제 상태 전이를 만들면(`likes` DELETE 영향 행 수 `1`) `delta = -1` 이벤트를 남긴다. 이미 미좋아요 상태인 반복 `DELETE`처럼 영향 행 수가 `0`인 no-op 요청은 이벤트를 남기지 않는다.
- Kafka topic은 `catalog-events`, 이벤트 타입은 `LIKE_COUNT_CHANGED_V1`, Kafka key는 `productId`다. 동일 상품의 증감 이벤트는 같은 key로 보내 상품 단위 순서를 최대한 보존한다.
- 이벤트는 UUID `eventId`를 가진다. consumer는 `processed_kafka_events` 또는 동일 목적의 `event_handled` 테이블에 `(consumer_group, eventId)` 처리 기록을 저장하고, 같은 이벤트가 재전달되면 `product_metrics`에 `delta`를 다시 적용하지 않는다.
- `product_metrics`는 조회 성능을 위한 projection이므로 짧은 projection lag를 허용한다. consumer 중단, 재처리, 장애 복구 시에는 Kafka replay와 `likes` 기준 backfill/rebuild로 projection을 다시 맞춘다.
- 쿠폰 발급 요청은 이 좋아요 수 이벤트 흐름으로 옮기지 않고 `coupon-issue-requests` topic을 사용하는 별도 비동기 command 흐름으로 처리한다.

---

### 5.5 주문 (Orders)

#### 5.5.1 사용자 주문 API

| Method | URI | User Required | 설명 |
| :--- | :--- | :---: | :--- |
| `POST` | `/api/v1/orders` | O | 주문 요청 |
| `GET` | `/api/v1/orders?startAt=2026-01-31&endAt=2026-02-10` | O | 유저의 주문 목록 조회 |
| `GET` | `/api/v1/orders/{orderId}` | O | 단일 주문 상세 조회 |

##### 주문 요청 (POST `/api/v1/orders`) Body 예시

```json
{
  "paymentMethod": "CARD",
  "items": [
    { "productId": 1, "quantity": 2 },
    { "productId": 3, "quantity": 1 }
  ],
  "couponId": 42
}
```

외부 API 요청 필드명은 Round 4 원문 계약인 `couponId`를 사용한다. 이 `couponId`는 사용자가 보유한 발급 쿠폰 식별자이며, 쿠폰을 적용하지 않는 주문은 이 필드를 생략한다. 단 내부 command/domain/DB는 의미를 명확히 하기 위해 발급 쿠폰을 `issuedCouponId`(DB `issued_coupon_id`), 쿠폰 템플릿을 `couponTemplateId`로 분리해 표현한다. 즉 외부 `couponId` -> 내부 `issuedCouponId`로 매핑한다(`@JsonProperty("couponId") issuedCouponId`). 발급/관리자 경로 변수 `{couponId}`도 외부 표기일 뿐이며 내부에서는 쿠폰 템플릿 식별자(`couponTemplateId`)로 받는다(`@PathVariable("couponId") couponTemplateId`).

#### 5.5.2 관리자 주문 조회 API

| Method | URI | LDAP Required | 설명 |
| :--- | :--- | :---: | :--- |
| `GET` | `/api-admin/v1/orders?page=0&size=20` | O | 주문 목록 조회 |
| `GET` | `/api-admin/v1/orders/{orderId}` | O | 단일 주문 상세 조회 |

관리자 주문 조회는 `PAYMENT_PENDING`, `ORDERED`, `PAYMENT_FAILED` 주문을 모두 조회 대상으로 삼는다. 실패·중단 주문은 결제 장애, 외부 시스템 문제, 보상 처리 상태 확인을 위한 운영 이력이다.

#### 5.5.3 주문 처리 핵심 비즈니스 룰

> [!IMPORTANT]
> **주문 처리 핵심 비즈니스 룰:**
> 1. **결제 기록**: 후속 결제 연동 설계에서는 주문마다 하나의 결제 기록을 남긴다. 결제수단별 재시도, 다중 결제 이력, outbox 는 결제 연동 방식이 구체화되면 추가한다.
> 2. **데이터 스냅샷**: **주문 정보**에는 주문 시점의 상품명, 가격 등의 정보를 **스냅샷** 형태로 보관하여 상품 가격이 변하더라도 과거 주문 내역이 훼손되지 않아야 한다.
> 3. **쿠폰 적용**: `issuedCouponId`가 있으면 발급 쿠폰 소유자, 사용 상태, 유효기간, 최소 주문 금액을 검증하고 주문 할인 금액을 확정한다.
> 4. **재고·쿠폰 정합성**: 주문 시점에 **상품 재고 확인 및 차감**과 **쿠폰 사용 상태 변경**이 하나의 DB 트랜잭션에서 보장되어야 한다. 둘 중 하나라도 실패하면 주문 전체를 거부한다.
> 5. **락 전략**: 재고는 비관적 락, 발급 쿠폰은 낙관적 락으로 도메인 특성에 맞게 다르게 적용한다. 여러 상품 재고 행을 잠글 때는 교착을 막기 위해 상품 ID 정렬 순서로 `PESSIMISTIC_WRITE` 잠금을 획득한다. 발급 쿠폰은 비관적 락으로 함께 잠그지 않고, 같은 트랜잭션에서 사용 가능 상태(`AVAILABLE`)를 검증한 뒤 `USED`로 변경하며 `version` 낙관적 락으로 동시 사용을 감지한다. 자세한 근거는 5.5.6 동시성·락 전략을 참조한다.
> 6. **실패 이력 보존**: 결제 실패 주문은 삭제하지 않고 `PAYMENT_FAILED` 상태로 유지한다. API 응답은 성공 생성이 아니라 실패 응답이지만, 사용자는 주문 목록/상세에서 실패 이력을 확인할 수 있다.

#### 5.5.4 주문·결제 상태 정책

| 상태 | 의미 | 비고 |
| :--- | :--- | :--- |
| `PAYMENT_PENDING` | 주문이 생성되고 결제 결과를 기다리는 상태 | `OrderModel.create()`의 초기 상태 |
| `ORDERED` | 외부 결제가 승인되어 주문이 확정된 상태 | `PaymentStatus.APPROVED`와 대응 |
| `PAYMENT_FAILED` | 외부 결제가 실패하고 보상 처리가 완료된 상태 | 실패 주문 row와 금액 스냅샷은 보존 |
| `CANCELED` | 주문 취소 상태 | 이번 라운드에서는 확장 후보 |

후속 결제 연동 설계에서 허용하는 주문 상태 전이는 `PAYMENT_PENDING -> ORDERED`, `PAYMENT_PENDING -> PAYMENT_FAILED`다. `CANCELED` 전이는 취소·환불 정책이 확정되면 추가한다.

결제 상태는 `REQUESTED`, `APPROVED`, `FAILED`를 둔다. 주문 상태와 결제 상태의 기본 대응은 다음과 같다.

| 주문 상태 | 결제 상태 |
| :--- | :--- |
| `PAYMENT_PENDING` | `REQUESTED` |
| `ORDERED` | `APPROVED` |
| `PAYMENT_FAILED` | `FAILED` |

#### 5.5.5 트랜잭션·보상 원칙

이 절은 Payment 연동 구현 단계의 목표 보상 설계다. Round 4 구현 이슈에서는 쿠폰 검증, 재고 차감, 주문 저장, 발급 쿠폰 `USED` 전환이 하나의 DB 트랜잭션 안에서 원자적으로 처리되는지를 우선 검증한다.

외부 결제 호출은 DB 트랜잭션 밖에서 수행한다. 주문 처리 흐름은 다음 세 구간으로 나눈다.

1. **TX1**: 주문을 `PAYMENT_PENDING`으로 생성하고, 재고를 차감하며, 쿠폰 적용 주문이면 발급 쿠폰을 `USED`로 전환하고, 결제 기록을 `REQUESTED`로 저장한다.
2. **외부 호출**: 결제 게이트웨이를 DB 트랜잭션 밖에서 호출한다.
3. **TX2 승인**: 결제 기록을 `APPROVED`, 주문 상태를 `ORDERED`로 변경한다.
4. **TX2 실패 보상**: 결제 기록을 `FAILED`, 주문 상태를 `PAYMENT_FAILED`로 변경하고 재고를 복구한다. 쿠폰 적용 주문이면 발급 쿠폰을 `AVAILABLE`로 복구하고 실패 주문의 `issuedCouponId`를 `NULL`로 분리한다.

TX2 실패 보상은 주문 상태가 `PAYMENT_PENDING`일 때만 수행한다. 이 상태 가드는 중복 콜백이나 향후 orphan 회복 프로세스가 재고와 쿠폰을 이중 복구하는 것을 막는다.

결제 실패 주문의 `discountPrice`는 시도 당시 할인 계산 스냅샷이다. 보상 후 `issuedCouponId = NULL`이더라도 `discountPrice > 0`일 수 있으며, 이는 쿠폰 점유가 해제된 실패 이력이라는 뜻이다.

결제 승인 거절이나 외부 결제 장애가 발생하면 주문 row는 `PAYMENT_FAILED`로 보존하지만, `POST /api/v1/orders` 응답은 성공 생성(`201`)이 아니다. 결제 승인 거절은 `409 Conflict`, 외부 결제 장애나 타임아웃은 `502 Bad Gateway`로 응답한다.

#### 5.5.6 동시성·락 전략

동시성 제어는 단일 메커니즘을 강제하지 않고 유스케이스별 특성에 맞춰 선택한다. 각 로직이 무슨 락/제약을 왜 선택했는지는 다음 표를 단일 기준으로 삼는다.

| 유스케이스 | 동시성 위험 | 선택한 락/제약 | 근거 |
| :--- | :--- | :--- | :--- |
| 좋아요 등록/취소 | 중복·연타·동시 요청 | DB 복합 PK + 조건부 INSERT/DELETE (행 락 없음) | 멱등 연산이라 락 불필요. 멱등성은 `(user_id, product_id)` PK가 보장 |
| 좋아요 수 projection | Kafka 재전달·consumer 장애·짧은 조회 지연 | `likes` source of truth + `LIKE_COUNT_CHANGED_V1` + `processed_kafka_events` 또는 `event_handled` dedupe | 실제 INSERT/DELETE 영향 행 수가 1일 때만 `delta=+1/-1` 이벤트를 남기고, `productId` key로 발행한다. consumer는 UUID `eventId` 처리 이력을 저장한 뒤 `product_metrics.like_count` projection을 갱신한다. projection lag는 허용하며 replay/backfill로 복구한다 |
| 쿠폰 발급 요청 | 중복 요청·Kafka 재전달·선착순 수량 경합 | 요청 상태 테이블 + outbox + `coupon-issue-requests` + worker 멱등 처리 | API는 요청 저장과 outbox 저장까지만 원자적으로 처리하고 `202 Accepted`를 반환한다. worker는 별도 트랜잭션에서 요청 상태를 확정하며 event id 처리 이력, 사용자-템플릿 unique, 수량 차감 락/조건부 update로 중복 발급과 초과 발급을 막는다 |
| 쿠폰 사용 | 동일 쿠폰 동시 주문으로 중복 사용 | 낙관적 락(`version`) + 상태 검증(`AVAILABLE`/`used_at IS NULL`) | 경합 주체가 소유자 본인으로 한정돼 충돌 확률이 낮아 비관적 락 대비 응답이 빠르고 재처리가 단순. `version`은 lost update를, 상태 검증은 재사용을 막으며 둘이 함께 작동해야 "단 1회 사용"이 보장됨. 충돌 시 주문 실패로 응답 |
| 재고 차감 | 동시 주문으로 초과 차감·음수 재고 | 비관적 락(`PESSIMISTIC_WRITE`) + 상품 ID 정렬 잠금 | 다중 상품 행을 read-modify-write로 차감하므로 경합이 잦고 정렬 잠금으로 교착을 방지. 차감은 잔여 재고 검증 후 수행 |

> [!NOTE]
> 재고(비관)와 쿠폰(낙관)이 한 주문 트랜잭션에 공존하는 것은 의도된 혼합이다. 재고는 다중 행 동시 차감으로 경합이 잦지만 쿠폰 사용은 소유자 단독 토글이라 경합이 드물어, 각 도메인 특성에 최적인 전략을 독립적으로 선택한 결과다.

---

### 5.6 쿠폰 (Coupons)

#### 5.6.1 사용자 쿠폰 API

| Method | URI | User Required | 설명 |
| :--- | :--- | :---: | :--- |
| `POST` | `/api/v1/coupons/{couponId}/issue` | O | 쿠폰 발급 |
| `GET` | `/api/v1/coupons/issue-requests/{requestId}` | O | 쿠폰 발급 요청 상태 조회 |
| `GET` | `/api/v1/users/me/coupons` | O | 내 발급 쿠폰 목록 조회 |

> [!IMPORTANT]
> 내 쿠폰 목록은 Round 4 원문 경로인 `/api/v1/users/me/coupons`를 사용한다. 사용자 식별자는 path가 아니라 인증 컨텍스트에서 얻는다.

#### 5.6.2 관리자 쿠폰 API

| Method | URI | LDAP Required | 설명 |
| :--- | :--- | :---: | :--- |
| `GET` | `/api-admin/v1/coupons?page=0&size=20` | O | 쿠폰 템플릿 목록 조회 |
| `GET` | `/api-admin/v1/coupons/{couponId}` | O | 쿠폰 템플릿 상세 조회 |
| `POST` | `/api-admin/v1/coupons` | O | 쿠폰 템플릿 생성 |
| `PUT` | `/api-admin/v1/coupons/{couponId}` | O | 쿠폰 템플릿 수정 |
| `DELETE` | `/api-admin/v1/coupons/{couponId}` | O | 쿠폰 템플릿 soft delete |
| `GET` | `/api-admin/v1/coupons/{couponId}/issues?page=0&size=20` | O | 쿠폰 템플릿별 발급 이력 조회 |

##### 쿠폰 템플릿 생성/수정 Body 예시

```json
{
  "name": "WELCOME_10",
  "type": "RATE",
  "value": 10,
  "minOrderAmount": 30000,
  "expiredAt": "2026-12-31T23:59:59"
}
```

> [!NOTE]
> 외부 API 요청 바디는 채점 호환성을 위해 Round 4 원문의 `type: "FIXED" | "RATE"`와 `value`를 기본 계약으로 둔다. 내부 command/domain/DB 매핑에서는 의미를 명확히 하기 위해 `FIXED -> FIXED_AMOUNT`, `RATE -> PERCENTAGE`, `value -> discountValue`로 변환할 수 있다.

> [!NOTE]
> 쿠폰 템플릿 수정은 향후 발급·사용 판단에 적용된다. 이미 생성된 주문의 `discountPrice`와 주문 항목 스냅샷은 변경하지 않는다.

#### 5.6.3 쿠폰 발급 규칙

- 삭제된 쿠폰 템플릿은 발급할 수 없다.
- 유효기간이 지난 쿠폰 템플릿은 발급할 수 없다.
- 한 사용자는 같은 쿠폰 템플릿을 한 번만 발급받을 수 있다.
- `POST /api/v1/coupons/{couponId}/issue`는 실제 발급 완료가 아니라 발급 요청 접수 API다. 성공적으로 접수되면 `202 Accepted`와 `requestId`, 현재 status(`PENDING` 등)를 반환한다.
- API 트랜잭션은 쿠폰 템플릿 기본 검증, 발급 요청 저장, `coupon-issue-requests` outbox row 저장까지만 원자적으로 처리한다.
- `coupon-issue-requests` worker는 `commerce-api` command owner 쪽에서 별도 트랜잭션으로 실제 발급을 수행하고 요청 상태를 `ISSUED`, `DUPLICATE`, `SOLD_OUT`, `FAILED` 중 하나로 확정한다.
- 같은 사용자와 같은 쿠폰 템플릿의 중복 요청은 기존 `PENDING`/최종 요청을 반환하는 멱등 흐름을 우선한다. 실제 발급 중복은 DB unique 제약이 최종 방어선이다.
- 선착순 수량 제한은 worker 트랜잭션에서 쿠폰 템플릿 수량 잠금 또는 조건부 차감으로 보장한다. Kafka 재전달은 처리 이력 테이블로 멱등하게 처리한다.
- 사용자는 polling API로 발급 결과를 확인한다. 요청 접수 응답을 실제 쿠폰 발급 성공으로 해석하지 않는다.

#### 5.6.4 쿠폰 사용·조회 규칙

- 저장 상태가 `USED`이면 `USED`로 응답한다.
- 저장 상태가 `AVAILABLE`이고 현재 시각이 `expiredAt` 이후이면 `EXPIRED`로 응답한다.
- 저장 상태가 `AVAILABLE`이고 현재 시각이 `expiredAt` 이전이면 `AVAILABLE`로 응답한다.
- 주문 요청의 `issuedCouponId`가 요청 사용자 소유가 아니면 자원 존재 여부를 숨기기 위해 외부 응답은 `404 Not Found`로 반환한다.
- 주문에 적용된 발급 쿠폰은 TX1에서 `USED`로 전환한다.
- 결제 실패 보상 시 쿠폰 적용 주문이면 발급 쿠폰을 `AVAILABLE`로 되돌린다. 동시에 실패 주문의 `issuedCouponId`를 `NULL`로 분리해 같은 쿠폰의 재주문을 허용한다.

#### 5.6.5 쿠폰 할인 정책

- 외부 API의 `value`는 정수로 요청하고 DB에는 `discount_value` 정수 컬럼으로 저장한다.
- 외부 API의 `FIXED`는 내부 할인 정책 `FIXED_AMOUNT`에 대응하며 원 단위 정액 할인 금액이다. 값은 0보다 커야 한다.
- 외부 API의 `RATE`는 내부 할인 정책 `PERCENTAGE`에 대응하며 1~100 범위의 퍼센트 정수다.
- 정률 할인은 `BigDecimal`로 중간 계산한 뒤 `RoundingMode.FLOOR`로 원 단위 정수화한다.
- 최종 할인 금액은 주문 총액을 초과할 수 없다.
- 도메인 계층에서는 외부 `type + value`를 내부 `couponType + discountValue`로 매핑한 뒤 `sealed DiscountPolicy` 전략으로 변환해 할인 계산을 수행한다. 영속성 계층은 단일 `coupon_templates` 테이블을 유지하고 JPA 상속 매핑은 사용하지 않는다.

#### 5.6.6 운영 감사 확장 후보

이번 라운드에서는 별도 쿠폰 사용기록 테이블을 만들지 않는다. 현재 사용 여부는 `issued_coupons.status`, `issued_coupons.used_at`, 성공 주문의 `orders.issued_coupon_id`로 판단한다.

운영 감사나 쿠폰 사용 시도 이력 보존이 필요해지면 append-only `coupon_usage_records`를 검토한다. 후보 컬럼은 `coupon_usage_record_id`, `issued_coupon_id`, `coupon_template_id`, `user_id`, `order_id`, `discount_amount`, `used_at`이다. 주문은 여러 상품을 가질 수 있으므로 `product_id`는 기본 컬럼으로 두지 않는다.

---

### 5.7 이벤트 아키텍처와 상품 지표

Round 7 이벤트 아키텍처는 로컬 트랜잭션 경계 분리와 시스템 간 전파를 구분한다.

#### 책임 경계

- `commerce-api`는 도메인 이벤트 계약, `ApplicationEvent` 발행, `@TransactionalEventListener` 기반 후속 처리, outbox 저장, Kafka topic/key 라우팅을 소유한다.
- streamer 애플리케이션은 Kafka record 수집, 처리 이력 저장, projection upsert만 수행한다. 권위 상태 변경과 쿠폰 발급 비즈니스 규칙은 복제하지 않는다.
- 이벤트 계약은 현재 애플리케이션 경계 안에 둔다. 별도 공유 이벤트 모듈을 새로 만들지 않는다.

#### 로컬 이벤트

- 주문 생성, 결제 승인/실패, 상품 조회, 좋아요 변경, 쿠폰 발급 요청/완료/실패처럼 주 행위와 부가 처리를 나눌 필요가 있는 지점에서 Spring `ApplicationEvent`를 발행한다.
- 커밋된 상태만 관찰해야 하는 부가 로그, outbox 생성, 외부 전파 준비는 `@TransactionalEventListener(phase = AFTER_COMMIT)`를 사용한다.
- 롤백된 성공 행위는 성공 로그나 Kafka 이벤트를 만들지 않는다. 실패 자체를 기록해야 하는 별도 요구가 있을 때만 실패 이벤트/로그를 분리한다.

#### Kafka/outbox 정책

- 시스템 간 전파가 필요한 이벤트는 먼저 `outbox_events`에 durable row로 저장하고, relay가 DB 트랜잭션 밖에서 Kafka로 발행한다.
- producer는 `acks=all`, `idempotence=true`를 전제로 한다.
- 주요 topic은 `catalog-events`(상품 조회·좋아요·재고/카탈로그 지표, key=`productId`), `order-events`(주문·결제 결과, key=`orderId`), `coupon-issue-requests`(쿠폰 발급 command, key=`couponId`)다.
- consumer는 manual ack를 사용하고, DB commit 이후에 ack한다. 같은 event id 재전달은 `processed_kafka_events` 또는 동일 목적의 `event_handled` 테이블로 멱등 처리한다.
- `product_metrics(product_id, like_count, sales_count, view_count, last_event_at, last_like_event_at, last_sales_event_at, last_view_event_at, updated_at)`는 `product_like_counts`를 대체/흡수하는 상품 지표 projection이다. stale event는 지표별 마지막 발생시각 기준으로 방어한다.

---

## 6. 심화 과정

> [!TIP]
> **성장과 심화를 위한 탐색 주제:**
> 모든 기능의 정상적인 비즈니스 로직 구현을 완료한 후, 실제 대규모 상용 서비스에서 마주칠 수 있는 복잡한 문제들을 깊이 탐구하고 해결 방법을 모색해 본다.
>
> * **동시성 제어 (Concurrency Control)**: 동일한 상품에 동시 다발적인 대량의 주문 및 재고 차감 요청이 인입될 때 데이터의 정확성을 보장하는 방법 (낙관적/비관적 락, 분산 락 등)
> * **락 순서와 데드락 관리**: 여러 상품 재고 행과 발급 쿠폰 행을 함께 잠그는 주문에서 일관된 잠금 순서, 타임아웃, 재시도 정책을 검토한다.
> * **쿠폰 만료 조회 성능**: `EXPIRED`를 저장 상태로 두지 않고 조회 시 계산하므로, 발급 쿠폰 데이터가 누적되면 만료 조건 인덱스나 배치 전환을 검토한다.
> * **멱등성 보장 (Idempotency)**: 동일한 주문 요청 네트워크, 결제와 같은 외부 연동 작업 시 요청이 중복해서 수신되는 경우에 대한 안전 장치 설계
> * **분산 시스템 데이터 일관성 (Consistency)**: 여러 서비스 간 트랜잭션 롤백 및 보상 트랜잭션 기법 연구
> * **외부 IO와 트랜잭션 경계**: 외부 결제 호출은 분산 환경 여부와 무관하게 DB 트랜잭션 밖에서 수행해야 한다. 이를 `TX1 → 외부 호출 → TX2` 로 분리하면 *외부 호출 직후 프로세스 종료* 같은 경계 실패 시 주문 `PAYMENT_PENDING`, 결제 `REQUESTED`, 차감된 재고, `USED` 쿠폰이 남을 수 있다. 미결 회복 프로세스는 결제 상태를 확인한 뒤 주문 실패 전이, 재고 복구, 쿠폰 복구, 주문-쿠폰 연결 해제를 멱등하게 수행해야 한다. 외부 시스템 웹훅 수신, outbox 패턴 같은 회복 전략은 결제수단과 연동 방식이 구체화될 때 도입한다.
> * **조회 성능 최적화 (Slow Query Optimization)**: 상품 목록 필터링 및 대량의 주문 목록 조회 쿼리에 대한 최적의 인덱스 설계와 튜닝
> * **상품 지표 projection 운영**: `product_metrics`는 좋아요·판매·조회 이벤트를 Kafka consumer가 갱신하는 projection이므로 consumer lag, replay, `processed_kafka_events`/`event_handled` dedupe, 원천 테이블 기준 backfill/rebuild 절차를 함께 관리한다.
> * **동시 대량 주문 처리**: 핫스팟 상품에 대한 선착순 주문 및 트래픽 폭주 대응 방안
